# GitHub Personal Access Token (required)
# Create at: https://github.com/settings/tokens
# Needs no special scopes for public repos
GITHUB_TOKEN=your_github_token_here

# Turso database (optional — falls back to local SQLite file:./data/repos.db)
# 1. Sign up free at https://turso.tech
# 2. Create a database: turso db create repo-tracker
# 3. Get URL:   turso db show repo-tracker --url
# 4. Get token: turso db tokens create repo-tracker
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=your_turso_auth_token

# LLM enrichment (optional)
ANTHROPIC_API_KEY=your_anthropic_key_here

# NEO approach generation (optional)
OPENROUTER_API_KEY=your_openrouter_key_here

# "Solve with New" integration (optional)
NEXT_PUBLIC_ENABLE_NEW_INTEGRATION=false
NEXT_PUBLIC_NEW_TOOL_URL=https://your-new-tool.example.com

# ── Sync schedule ─────────────────────────────────────────────────────────────
# Two-phase adaptive schedule:
#   Phase 1 — first 48 hours after first deployment (backfill)
#   Phase 2 — steady-state after 48 hours
#
# Phase 1 interval in hours (default: 2.5 h → syncs ~19x in first 2 days)
SYNC_INTERVAL_PHASE1_HOURS=2.5
#
# Phase 2 interval in hours (default: 12 h → twice daily)
SYNC_INTERVAL_PHASE2_HOURS=12
#
# Set to 'false' to skip the sync that runs 5 s after server start
SYNC_ON_STARTUP=true

# Port (default: 3000)
# PORT=3000
